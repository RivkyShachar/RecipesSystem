namespace RecipesSystem.AppServer.Models
{
    public class ImageAdapter
    {
        public bool CheckIsFood(string ImageURL)
        {
            //ask the gateway server if the url is food
            return false;
        }
    }
}
