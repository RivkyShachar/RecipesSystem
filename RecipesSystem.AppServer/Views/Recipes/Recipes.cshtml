@model IEnumerable<RecipesSystem.AppServer.Models.Recipe>

@{
    ViewData["Title"] = "Recipes";
    Layout = "~/Views/Shared/LustigLayout.cshtml";
}

<!-- main -->
<main class="page">
    <section class="recipes-container">
        <!-- tag container -->
        <div class="tags-container">
            <h4>recipes</h4>
            <div class="tags-list">
                <a href="@Url.Action("TagTemplate", "Recipes") ">Beef (1)</a>
                <a href="@Url.Action("TagTemplate", "Recipes") ">Breakfast (2)</a>
                <a href="@Url.Action("TagTemplate", "Recipes") ">Carrots (3)</a>
                <a href="@Url.Action("TagTemplate", "Recipes") ">Food (4)</a>
            </div>
        </div>
        <!-- end of tag container -->
        <!-- recipes list -->
       <div class="recipes-list">
            <!-- single recipe -->
           @foreach (var item in Model)
           {
            <a href="@Url.Action("SingleRecipe", "Recipes") " class="recipe">

                    <img src=" @Url.Content(@item.ImageURL)" /
                     class="img recipe-img"
                     alt="" />
                    <h5> @Html.DisplayFor(modelItem => item.Description)</h5>
                    <p>Prep : @Html.DisplayFor(modelItem => item.TimeToMake) | Cook : @Html.DisplayFor(modelItem => item.CookingTime)</p>
            </a>
           }
    
        </div>
        
    </section>
</main>
<!-- end of main -->
